# Changelog

## v1.1.0
- Added `pluralize` to formatters
- Added `randomColor` to embeds
- Added `safeUnban` to moderation
- Added `isValidDuration`, `isValidRole`, `clamp` to validators
- Added `msToSeconds`, `daysUntil` to time
- Added `RateLimitError`, `NotInGuildError` to errors
- Added `getTag`, `stripMarkdown` to formatters